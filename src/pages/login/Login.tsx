import { Stack } from "@mantine/core";
import Authentication from "./Authentication";

const login = () => {
  return (
    <Stack>
      <Authentication />
    </Stack>
  );
};

export default login;
